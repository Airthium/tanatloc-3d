const style: { [key: string]: React.CSSProperties } = {
  canvas: {
    position: 'absolute',
    top: 0,
    right: 0,
    width: 'calc(100% - 200px)',
    height: '100%',
    zIndex: 9
  }
}

export default style
