const style: Record<string, React.CSSProperties> = {
  canvas: {
    position: 'absolute',
    top: 0,
    right: 0,
    width: '100vw',
    height: '100vh',
    overflow: 'hidden'
  }
}

export default style
