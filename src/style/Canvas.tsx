const style: { [key: string]: React.CSSProperties } = {
  canvas: {
    position: 'absolute',
    top: 0,
    right: 0,
    width: ' 80vw',
    height: '100vh',
    overflow: 'auto',
    touchAction: 'auto'
  }
}

export default style
